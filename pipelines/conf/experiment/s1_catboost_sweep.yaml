# @package _global_

# This experiment focuses purely on optimizing the Stage 1 Sieve Classifier.

defaults:
  - override /hparams_search: s1_catboost_career_threshold

# --- Metadata & Logging ---
experiment_name: "S1_Catboost_Hyperopt_v1"
quiet_sweep: true

# --- Optimization Target ---
optimization:
  metric: "s1_oof_pr_auc"
  direction: "maximize"

# --- Hydra Sweeper Settings ---
hydra:
  sweeper:
    n_trials: 100
    direction: ${optimization.direction}
    study_name: s1_catboost_tuning_v1
    storage: sqlite:///fof8-ml/optuna.db
