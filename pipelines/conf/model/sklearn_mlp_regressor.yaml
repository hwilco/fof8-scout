name: "sklearn_mlp_regressor"
params:
  hidden_layer_sizes:
    - 128
    - 64
  activation: "relu"
  solver: "adam"
  alpha: 0.0001
  learning_rate_init: 0.001
  max_iter: 500
  early_stopping: true
  validation_fraction: 0.15
  n_iter_no_change: 25
