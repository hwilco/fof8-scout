name: "sklearn_mlp_regressor"
params:
  hidden_layer_sizes:
    - 256
    - 128
  activation: "relu"
  solver: "adam"
  alpha: 0.001
  learning_rate_init: 0.0007
  batch_size: 128
  max_iter: 600
  early_stopping: true
  validation_fraction: 0.15
  n_iter_no_change: 30
  timing_diagnostics: false
